# 种花小游戏（Phaser 3）

一个基于 `Phaser 3` 的轻量种花养成小游戏。

玩家可在花盆里播种并收花，获得经验升级、解锁花盆；也可以通过交易和快捷功能提升效率。

## 当前功能

- 花种：`玫瑰`、`雏菊`、`牡丹`（可播种）
- 特殊花：`金花`（不可手动播种，只能由牡丹突变获得）
- 每株花 `5` 阶段成长，点击花或花盆可交互
- 生长间隔为随机 `10~15` 秒（每次阶段推进随机）
- 普通收花随机 `1~3` 朵
- 等级系统：收花获得经验，升级逐步解锁花盆
- 房子按钮弹窗：查看总收花并进行交易
- 右上快捷按钮：一键施肥 / 一键浇水 / 一键播种 / 一键采摘
- 每日任务：经验卡右侧独立任务卡片 + 任务弹窗领取奖励
- 舞台入口：种子工具栏中的"送给明星"卡片
- 花农入口：房子左侧花农 NPC，点击打开金花兑换弹窗
- 花仙子入口：房子右上方花仙子 NPC，点击打开帮助弹窗
- 左上信息区：经验卡 + 独立可折叠仓库卡
- 启动加载页：首屏显示进度条，单次会话内仅显示一次
- 弹窗动效：收花统计 / 每日任务 / 花农兑换支持打开关闭动画
- 折叠动效：仓库卡片支持平滑展开与收起
- 花盆状态持久化：切换页面或关闭后花盆中的花朵状态自动保存恢复
- 页面离开自动保存：`beforeunload` 和 `visibilitychange` 事件触发时自动存档
- 高清渲染：Phaser 3 原生支持 `antialias` 和高 DPI 显示设备，LINEAR 纹理过滤优化
- 新手引导：首次游戏时显示轮播式引导页面，花仙子带领玩家了解基本玩法
- 玩家昵称：点击经验面板顶部名字可自定义昵称（最多 8 字符）
- 音效系统：播种、生长、升级都有对应的音效
- 禁用右键菜单

## 关键玩法规则

### 升级与花盆

- 初始解锁花盆：`4`
- 花盆总数：`20`
- 每级经验：`100`
- 每朵花经验：`5`
- 最高等级：`17`
- 满级后经验封顶并显示"已满级，已解锁全部花盆"
- 未解锁花盆使用专用贴图：`img/背景/未解锁花盆.png`

### 牡丹突变金花

- 配置位于 `js/config.js -> MUTATION`
- 当前概率：`peonyToGoldenChance = 0.01`（即每次牡丹阶段推进有 1% 概率突变）
- 触发时机：牡丹在生长推进过程中（自然成长 / 一键浇水 / 一键施肥）
- 金花收获结算：`10` 朵牡丹 + `1` 朵金花

### 交易系统

在房子弹窗中消耗花朵兑换道具：

- `50` 朵花兑换 `1` 个 `浇水壶`
- `100` 朵花兑换 `1` 个 `化肥`
- `500` 朵花永久解锁 `一键播种`
- `500` 朵花永久解锁 `一键采摘`

当花朵不足时，兑换按钮会禁用。

### 快捷按钮（右上）

- 一键施肥：消耗 `化肥 x1`，所有已种花朵直接成熟
- 一键浇水：消耗 `浇水壶 x1`，所有已种花朵前进一个阶段
- 一键播种：需先永久解锁，对所有空花盆播种当前选中花种
- 一键采摘：需先永久解锁，采摘所有成熟花朵

### 每日任务

- 任务配置：`js/config.js -> DAILY_TASK`
- 当前任务：每日累计收花 `30` 朵
- 当前奖励：`化肥 x1`
- 展示方式：经验卡右侧独立"每日任务"卡片
- 点击卡片打开任务弹窗，满足条件后可领取
- 任务状态按自然日重置（本地时间），并持久化到本地存档

### 金花兑换

- 入口位置：房子左侧花农 NPC（`img/npc/花农2.png`）
- 兑换规则：`500` 朵玫瑰 `或` `500` 朵雏菊可兑换 `1` 朵金花
- 牡丹不参与兑换
- 点击花农打开卡片式兑换弹窗（玫瑰兑换 / 雏菊兑换）
- 弹窗标题为"花农兑换"，具体兑换按钮按库存独立禁用

### 送给明星页面

- 入口位置：种子工具栏中的"送给明星"卡片
- 点击后跳转到独立页面：`donate.html`
- 页面背景使用 `img/背景/加载背景.png`，固定不随滚动
- 圆形头像，自适应网格布局
- 每位明星下方有加减按钮选择赠送数量
- 白色不透明卡片，深色文字
- 顶部导航栏：左侧"返回花园"按钮 + 右侧金花数量徽章
- 打赏后实时扣减 `warehouse.golden` 并保存到游戏存档
- 明星打赏次数独立持久化到 `localStorage` 键：`flower_donate_records_v1`

### 收花统计（房子弹窗）

- 顶部显示累计收花总数
- 明细展示各花种累计数量：`玫瑰 / 雏菊 / 牡丹 / 金花`
- 弹窗支持滚动，适配小屏设备

### 仓库卡片

- 仓库从经验卡中独立为单独卡片
- 默认折叠，点击标题可展开/收起

### 新手引导

- 入口位置：`guide.html`，首次游玩时自动跳转
- 轮播式引导，共 5 个步骤
- 每个步骤配有说明图片（`img/guide/1-5.png`）
- 花仙子角色带浮动动画
- 支持上一步/下一步导航
- 支持跳过引导（自定义确认弹窗，非浏览器原生）
- 支持键盘导航（方向键、Enter键）
- 完成引导后标记状态，下次不再显示

### 音效系统

- 播种音效：`Sound effect/planting.wav` - 点击空花盆播种时播放
- 生长音效：`Sound effect/growup.wav` - 花朵自然生长、一键浇水、一键施肥时播放
- 升级音效：`Sound effect/levelup.wav` - 升级时播放（包括满级）

### UI 定位优化

- 左右两侧按钮基于屏幕中心线定位
- 使用 `left: 50%` 配合 `transform: translateX()` 实现
- 适配各种屏幕尺寸和长宽比
- 按钮位置居中分布，不会超出屏幕范围

## 本地运行

这是纯前端静态项目，使用任意静态服务器启动即可：

1. VS Code `Live Server`
2. `npx serve`

然后访问 `index.html`。

## 存档说明

使用 `localStorage`，键名：`flower_grade_save_v1`

主要字段：

- `level` / `exp`
- `warehouse`（含 `rose` / `daisy` / `peony` / `golden`）
- `selectedFlowerType`（仅可播种花种）
- `unlockedPotCount`
- `tools`
  - `wateringCan`（数量）
  - `fertilizer`（数量）
  - `oneClickPlant`（是否永久解锁）
  - `oneClickHarvest`（是否永久解锁）
- `dailyTask`
  - `dateKey`
  - `collectProgress`
  - `rewardClaimed`
- `potStages`（花盆阶段数组，`-1` 未解锁 / `0` 空花盆 / `1~5` 生长阶段）
- `potFlowerTypes`（花盆花种数组，`null` 或花种 id）
- `playerName`（玩家昵称，最多 8 字符）

其他本地存储：

- `flower_donate_records_v1`：记录每位明星累计收到的金花数量
- `flower_guide_completed`：记录新手引导是否已完成
- `flower_boot_loaded`：记录加载页是否已显示（单次会话）

## 项目结构

```text
flower_grade/
├─ index.html
├─ guide.html             (新手引导页面)
├─ donate.html
├─ phaser3.min.js        (Phaser 3.80.1)
├─ css/
│  └─ game.css
├─ js/
│  ├─ config.js          (游戏配置常量)
│  ├─ state.js           (状态管理 + 存档)
│  ├─ ui-modal.js        (弹窗动画工具)
│  ├─ ui-level.js        (经验面板 + 改名)
│  ├─ ui-warehouse.js    (仓库卡片)
│  ├─ ui-toolbar.js      (种子工具栏 + 快捷按钮)
│  ├─ ui-collect.js      (收花统计弹窗 + 交易)
│  ├─ ui-daily.js        (每日任务弹窗)
│  ├─ ui-exchange.js     (花农兑换弹窗)
│  ├─ ui-help.js         (帮助弹窗)
│  ├─ ui.js              (UI 主入口)
│  ├─ flower-pot.js      (花盆游戏对象)
│  └─ game.js            (游戏主入口)
├─ img/
│  ├─ 人物形象/
│  ├─ 背景/
│  ├─ npc/
│  ├─ 玫瑰/
│  ├─ 雏菊/
│  ├─ 牡丹/
│  ├─ 金花/
│  ├─ 工具/
│  └─ guide/              (新手引导步骤图片)
└─ Sound effect/         (游戏音效文件)
```

## 更新日志

### 2026-05-13 音效系统与 UI 优化

**音效系统**

- 新增 `Sound effect/` 目录，包含 planting.wav / growup.wav / levelup.wav
- 播种时播放 planting.wav
- 花朵生长（自然/浇水/施肥）时播放 growup.wav
- 升级时播放 levelup.wav
- 音效对象通过 `window.FlowerGameSoundEffects` 全局共享

**UI 定位重构**

- 左右按钮改为基于屏幕中心线定位
- 使用 `left: 50%` + `transform: translateX()` 实现居中分布
- 移除响应式媒体查询，统一使用一套布局
- 适配各种屏幕尺寸和长宽比，按钮不会超出屏幕

**新手引导优化**

- 跳过引导的确认弹窗改为页面内自定义样式
- 新增 5 张引导步骤图片（`img/guide/1-5.png`）
- 弹窗自适应大小，支持图片展示
- 确认按钮有"继续引导"和"跳过引导"两个选项

**花仙子定位**

- 花仙子位置改为跟随房子位置
- 定位在房子的最右边上方
- 随窗口大小变化自动调整位置

**背景渲染优化**

- 背景图原点改为中心（`setOrigin(0.5, 0.5)`）
- 背景图始终居中显示
- 移除黑色背景色，改为透明
- body 背景也改为透明

### 2026-05-13 功能优化与新手引导

**新手引导系统**

- 新增 `guide.html` 独立新手引导页面
- 轮播式引导提示框，包含 5 个引导步骤
- 花仙子引导角色（`img/npc/花仙子.png`）带浮动动画
- 初次游玩判定，完成引导后标记状态
- 支持跳过引导（带确认提示）
- 键盘导航支持（方向键、Enter键）

**高清渲染优化**

- 升级抗锯齿配置，启用 WebGL 抗锯齿
- 添加 `resolution` 配置，适配不同屏幕 DPI
- 所有纹理设置为 LINEAR 过滤模式
- 优化图像渲染 CSS 样式
- 禁用自动取整，保持亚像素渲染精度

**视觉与交互优化**

- 花农 NPC 放大 20%（`npcFarmer` 缩放比例调整）
- `donate.html` 响应式优化，小屏幕自动适配
- 网格布局改为自适应模式，充分利用屏幕空间
- 页面容器宽度优化，避免横向滚动
- 头像尺寸响应式调整

**本地存储扩展**

- 新增 `flower_guide_completed` 本地存储字段
- 用于标记新手引导是否已完成

### 2026-05-12 大版本更新

**引擎升级**

- 升级 Phaser 2.6.2 → Phaser 3.80.1
- 启用硬件抗锯齿（`antialias: true`），改善渲染质量
- 场景化架构重写，所有 API 迁移到 Phaser 3

**花盆状态持久化**

- 新增 `potStages` / `potFlowerTypes` 存档字段
- 切换页面或关闭浏览器后，花盆中的花朵状态自动保存
- 页面离开时自动存档（`beforeunload` + `visibilitychange`）

**玩家昵称系统**

- 经验面板顶部可点击改名
- 自定义内联输入框（非浏览器原生 prompt）
- 最多 8 字符，XSS 过滤防护

**送给明星页面重构**

- 圆形头像，自适应网格布局
- 加减按钮选择赠送数量，确认赠送
- 白色不透明卡片，深色文字
- 顶部导航栏集成"返回花园"按钮和金花数量
- 背景固定不随滚动

**UI 调整**

- 右上快捷按钮、左上经验面板和仓库面板定位优化
- 保持合理间距

**收花统计弹窗优化**

- 弹窗支持滚动，适配小屏设备

**安全与体验**

- 全局禁用鼠标右键菜单
- 玩家昵称 XSS 过滤

**代码重构**

- `ui.js` 拆分为 8 个模块
- 全部 JS 文件添加 JSDoc 注释
- 模块化架构，职责清晰
